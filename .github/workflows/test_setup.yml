name: Test deployment recipe workflow

on:
  pull_request:  # TODO: For debugging - remove before merge
  workflow_call:
  workflow_dispatch:
  schedule:
    - cron: '0 6,18 * * *'

jobs:
  latest:
    runs-on: ubuntu-latest

    steps:
      - name: Set up full_stack
        uses: ./.github/actions/test-setup
